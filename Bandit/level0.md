# solution;
  - first what is ssh?
    - it's a secure shell host protocol that allows user to communicate with other server through the network but it's data is encrypted which means no hacking stuff can access it.
    - it's working on port tcp 22
    -it's command to access another machine; ssh username@hostnameoripaddress -p portnumber.
ZjLjTmM6FvvyRnrb2rfNWOZOTa6ip5If
    
    
